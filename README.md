# data branch

This branch stores time-series data collected by the GitHub Actions workflow
defined on `main` (`.github/workflows/toyenbadet-tracker.yml`).

- `data/toyenbadet.json` — array of `{timestamp, datelong, current, visitorsIn, visitorsOut, capacityStatus}` entries.

Do not edit by hand.
